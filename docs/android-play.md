# Android packaging notes

See **[PLAY_STORE.md](./PLAY_STORE.md)** for Play Console upload steps and store listing copy.

Quick rebuild:

```bash
npm run android:bundle
npm run android:apk
```

Artifacts land in `releases/play/<version>/`.
